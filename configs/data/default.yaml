data:
  raw_dir: "data/raw"
  processed_dir: "data/processed"
  evaluation_dir: "data/evaluation"
  # Motor OD document verification SFT (from verify_docs_sft_500.jsonl)
  train_file: "data/processed/train.jsonl"
  eval_file: "data/processed/eval.jsonl"
  usecase_eval_file: "data/evaluation/usecase_eval.jsonl"
  # Held-out demo split for base vs FT report (not used in fine-tuning)
  comparison_eval_file: "data/evaluation/heldout_comparison_eval.jsonl"
  instruction_template: "sharegpt"   # alpaca | chatml | sharegpt (messages[])
  max_samples: null                # null = use all
  validation_split: 0.1
