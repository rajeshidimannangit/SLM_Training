# Base models

Place or cache base SLM checkpoints here (or let Unsloth/Hugging Face cache them and record metadata via `slm download-base`).

Configured default: see `configs/model/default.yaml` → `model.local_base_dir`.
